# Contracts

A contract is simply a written agreement about how two parts exchange data.

Keep these definitions here:

- MQTT topic names;
- JSON fields sent by ESP8266;
- JSON fields used for control commands;
- units, timestamps, required fields, and message examples.

For the first version, Markdown files and example JSON files are enough. Both Arduino and Python implementations should follow the same definitions.
