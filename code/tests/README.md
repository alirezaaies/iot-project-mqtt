# Tests

Put tests that connect multiple project parts here, for example:

- MQTT message to SQLite storage;
- database data to Flask response;
- web control request to MQTT command;
- ESP8266 acknowledgement to command status.

Small tests for only one file may stay beside that component until a larger test structure is needed.
